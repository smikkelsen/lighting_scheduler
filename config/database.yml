default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  database: lighting_scheduler_development

test:
  <<: *default
  database: lighting_scheduler_test

production:
  <<: *default
  username: deploy
  password:
  database: lighting_scheduler